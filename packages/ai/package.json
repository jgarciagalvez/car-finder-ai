{
  "name": "@car-finder/ai",
  "version": "1.0.0",
  "description": "AI provider abstraction layer with Gemini API integration and future provider support",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:unit": "jest --testPathIgnorePatterns=integration",
    "test:integration": "jest --testPathPattern=integration",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@car-finder/types": "workspace:*",
    "@car-finder/services": "workspace:*",
    "@google/generative-ai": "~0.11.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.5.0"
  }
}
