{
  "name": "@car-finder/db",
  "version": "1.0.0",
  "description": "Database layer for Car Finder AI with SQLite and Kysely",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "lint": "echo 'ESLint configured - TypeScript linting via type-check'",
    "type-check": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@car-finder/types": "workspace:*",
    "@libsql/client": "^0.15.15",
    "kysely": "^0.27.0",
    "kysely-libsql": "^0.7.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.0.0"
  }
}
