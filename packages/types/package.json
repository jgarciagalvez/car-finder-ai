{
  "name": "@car-finder/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Car Finder AI",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo 'ESLint configured - TypeScript linting via type-check'",
    "type-check": "tsc --noEmit",
    "test": "jest"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
